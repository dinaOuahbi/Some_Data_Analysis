				FORMATION PYTHON 
	https://www.youtube.com/playlist?list=PLrSOXFDHBtfHg8fWBd7sKPxEmahwyVBkC
		----------------------------------------------------

python est un language interpreter car il a un interpreteur deriere qui va comprendre le code et l'executer ligne par ligne 
les language compiler vont a leur tours transformer dabord le code en binaire qui est un language machine, ensuite l'executer. se sont donc des language tres puissant et leger et sont par consequent utiliser dans des domaine qui demande bcp de calcule

le python existe sous plusieur vrsion, la derniere est la 3.10. on peut voir ca sur python.org
une fois installer, le python va directement integrer le path de l'ordinateur pour puvoir etre interpreter sur ligne de commande
pour activer le python, il suffit de taper 'python' sur cmd. et pour pouvoir quitter, nous tapons 'quit()' 
		----------------------------------------------------
pour commencer un script, le mieux est de definir l'encodage au debut
#coding:utf-8

ce type d'encodage est tres generale est compatible avec tt les language du monde que ce soit le chinoi, l'arabe ou le francais, les caractaire accentuer ect...

ensuite pour afficher un message, c'est entre double ou simple cote. mais double c'est mieux car de cette facon on pourra s'echaper au apostrophes dans une chaine de caractaire
pour un retour a la ligne, c'est l'anti slash suivit de n (pour new line)
pour une tabulation : \t

# ceci est un cmt simple ligne
''' 
ceci est un cmt en plusieur
ligne 
'''
		----------------------------------------------------
https://www.youtube.com/watch?v=rfscVS0vtbw&t=6932s

notes : 

Always typecast your input
Default input is always string

*List:
 List can store multiple data types at a time.
 List Function:
 f1.extend( f2 ) - adds two lists
 f1.insert( index, f1)- inserts an elements
 f1.remove(value) - removes the element
 f1.pop() - removes last element off the list
 f1.count(value)- counter 
 f1.reverse() - reverses the original order of the list
 f2=f1.copy() - will copy one list to another


*Tuple - can’t be changed once declared(same as list)
 Use normal brackets not square brackets
 ex_tuple = ( 4,5 ) 


*Function:
 Keyword - def
 Example -def ex_func():
 Every function needs to be called in order to be executed.


*IF
 or- || , and-&& , not()- negate 
 Syntax-
 if condition1 :
 elif condition2:
 else :


*Dictionaries:
 Can excess certain values by shortcuts or abbreviations
  ex_dic={
    “Jan” : “January” ,
     “Feb” : “February”
   }
  print(ex_dic(get( “Jan” , “ Invalid “ )))   // used get() to avoid invalid  inputs


*While:
 Syntax:
 while condition:
 {}


*For:
 Syntax:
 for variable in range( the range for loop):
 {}


*2D List:
 ex_2D = [ [1,2,3] ,\ [4,5,6] , [7,8,9] , [0] ]
 for row in ex_2D:
     for col in row:
         print(col)
 OUTPUT: 
 All elements one by one


*Comments:
 #anythinginsidethishashtagwillbeacomment 


*Try/except :if user enters a wrong input
  Syntax:
  try:
    #ifanyerroroccurshereexceptwillrun
  except typeoferror:
     #statements

*Read files
  If you open a file ,always close it.
  file=open( “file.txt” , “ r ” ) #thiswillreadthefile
  print( file.readable()) #readable()willgiveoutabooleanvalue
  file.close()
  readlines() - will convert each line to an index of an array 
 
*Write files
  file=open( “file.txt” , “ a ” ) #thiswillappend
  file=open( “file.txt” , “ w ” ) #thiswilloverwrite